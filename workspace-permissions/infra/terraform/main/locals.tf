# =============================================================================
# Locals - Workspace Permissions
# =============================================================================

locals {
  # Pass through permission assignments from variables
  permission_assignments = var.permission_assignments
}
