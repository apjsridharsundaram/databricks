# =============================================================================
# Provider Configuration - Workspace Permissions
# =============================================================================

provider "databricks" {
  alias = "workspace"
  # host  = var.databricks_workspace_url
  # token = var.databricks_workspace_token
}
